name: Multiple jobs
on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build step
        run: echo "A demo Build"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: test Phase
        run: echo "Running test cases"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy the App
        run: "Deploy the App"
