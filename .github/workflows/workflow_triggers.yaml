name: Workflow triggers
on:
  push:
    branches:
      - main
      - develop
      - 'release/**'
  pull_request:
    branches:
      - main
jobs:
  build_trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow triggers
        run: echo "Workflow Triggers"
  
